<%= render "navbar" %>

<div class="container">
  <h1 class="text-5xl text-center my-5">
    Drinkreen - The real plant based beer
  </h1>
  <div class="d-flex justify-content-center mb-4">
    <p class="lead text-center" style="max-width: 500px">
      Welcome to the place where vegans can drink any beer without choosing by
      ingredient, but by wonderful taste!
    </p>
  </div>

  <div class="row">
    <% @allBeers.each do | beer | %>
    <div class="col">
      <div class="card h-100">
        <div class="card-body d-flex flex-column">
          <h4 class="card-title mb-4"><%= beer.title %></h4>
          <a
            target="_blank"
            href="/beer/<%= beer.id %>"
            class="btn btn-primary mt-auto"
            >More!</a
          >
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
